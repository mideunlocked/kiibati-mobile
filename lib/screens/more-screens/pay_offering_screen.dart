import 'package:flutter/material.dart';

class PayOfferingScreen extends StatelessWidget {
  const PayOfferingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
